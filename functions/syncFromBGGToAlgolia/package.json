{
  "name": "syncFromBGGToAlgolia",
  "main": "index.js",
  "scripts": {
    "deploy": "gcloud functions deploy syncFromBGGToAlgoliaHTTP --runtime nodejs8 --trigger-http && gcloud functions deploy syncFromBGGToAlgoliaPubSub --runtime nodejs8 --trigger-resource SYNC_FROM_BGG_TO_ALGOLIA --trigger-event google.pubsub.topic.publish"
  },
  "dependencies": {
    "algoliasearch": "^3.30.0",
    "axios": "^0.18.0",
    "xml2js": "^0.4.19"
  }
}
